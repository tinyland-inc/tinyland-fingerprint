name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  actions: read
  contents: read
  packages: read
jobs:
  package:
    uses: tinyland-inc/ci-templates/.github/workflows/js-bazel-package.yml@282fbbf9e44f180bd0703f86275676eb444e971a
    with:
      runner_mode: repo_owned
      runner_labels_json: ${{ vars.PRIMARY_LINUX_RUNNER_LABELS_JSON }}
      workspace_mode: isolated
      publish_mode: hosted_exception
      node_versions: '["22"]'
      publish_node_version: "22"
      pnpm_version: "10.13.1"
      # npm-resolution retired; validate via bazel (TIN-1716)
      typecheck_command: ""
      unit_test_command: ""
      build_command: "true"
      package_check_command: "true"
      bazel_targets: "//:pkg //:test"
      package_dir: ./bazel-bin/pkg
      npm_access: public
      npm_publish_mode: disabled
      github_package_name: "@tinyland-inc/tinyland-fingerprint"
      dry_run: true
    secrets: inherit
